# Frontend environment variables
# Copy this to .env.local for local development

# In development: leave blank (Vite proxy handles /api/v1 → localhost:3000)
# In production:  set to your deployed backend URL
VITE_API_URL=
